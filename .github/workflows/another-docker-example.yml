name: another-docker-example

on: [push]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    name: Run unit tests
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build docker container
        uses: ./.github/actions/build-docker-image
        # Todo: run something in the docker container.